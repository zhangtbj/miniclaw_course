# MiniClaw course project dependencies
# Direct dependencies only; transitive deps are resolved by pip per platform
# (Windows / macOS / Linux). Python 3.11, see README.md / AGENTS.md.

crewai>=1.15.1          # Agent framework (test1-test5, llm/llm.py, tools/)
crewai-tools>=1.15.1    # FileWriterTool / FileReadTool (test4)
python-dotenv>=1.2.2    # loads root .env config
requests>=2.34.2        # HTTP calls in llm/llm.py and test6
PyYAML>=6.0.3           # SKILL.md frontmatter parsing (test5)
pydantic>=2.12.5        # custom tools (tools/) and structured output (test3)
